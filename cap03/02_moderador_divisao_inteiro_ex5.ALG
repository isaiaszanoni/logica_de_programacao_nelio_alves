Algoritmo "Exemplo 5 - Real para Inteiro sem perder info"


Var
   a : real
   b : inteiro

Inicio

   escreva("Atribuir a variável A (5.0) à variável B (n inteiro iria retornar")
   escreva("um erro de sintaxe. Façamos como o algoritmo abaixo:")
   escreval
   a <- 5.5
// ERRO   b <- a
   b <- int(a)


   escreval(b)
Fimalgoritmo